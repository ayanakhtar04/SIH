# Frontend environment example for PathKeepers dashboard
# Copy to .env or .env.local and adjust values.

# If set, Vite will proxy /api -> BACKEND_URL (see vite.config.js)
BACKEND_URL=http://localhost:5055

# Optional: separate auth service base (full URL) if auth runs on a different port
# When present, auth client will use this instead of `${API_BASE}/auth`.
VITE_AUTH_API_BASE=http://localhost:5055/api/auth

# Optional: override entire API base (students, train, etc.)
# VITE_API_BASE=/api
